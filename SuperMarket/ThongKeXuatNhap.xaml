<UserControl x:Class="SuperMarket.ThongKeXuatNhap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SuperMarket"
             mc:Ignorable="d" 
             d:DesignHeight="540" d:DesignWidth="1080">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="9*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Name="btnBH" Content="Bán Hàng" Width="150" Margin="20 0" Click="BtnBH_Click" IsEnabled="False"/>
                <Button Name="btnNH" Content="Nhập Hàng" Width="150" Margin="20 0" Click="BtnNH_Click"/>
                <Button Name="btnTH" Content="Tồn Hàng" Width="150" Margin="20 0" Click="BtnTH_Click"/>
            </StackPanel>
        </DockPanel>
        <DockPanel Grid.Row="1">
            <Grid Name="GridMain"/>
        </DockPanel>
    </Grid>
</UserControl>
